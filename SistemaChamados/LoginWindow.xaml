<Window x:Class="SistemaChamados.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - Inovatech"
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#B39DDB">

    <Grid>
        <Border Width="400" Height="400" Background="#8E44AD" CornerRadius="10"
        VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30" >
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <TextBlock Text="Inovatech" FontSize="32" FontWeight="Bold"
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Usuário:" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUsuario" Width="300" Height="35" FontSize="14" Margin="0,0,0,20"/>

                <TextBlock Text="Senha:" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtSenha" Width="300" Height="35" FontSize="14" Margin="0,0,0,30"/>

                <Button Content="Entrar"
        Width="120" Height="35" FontSize="14"
        Background="#B39DDB" Foreground="White"
        HorizontalAlignment="Center" Cursor="Hand"
        BorderThickness="0" Click="Entrar_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#3498DB"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="8"
                                SnapsToDevicePixels="True">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2980B9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1F618D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>
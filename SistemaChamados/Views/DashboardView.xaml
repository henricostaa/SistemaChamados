<Window x:Class="SistemaChamados.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaChamados.Views"
        mc:Ignorable="d"
        Title="DashboardView" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Painel Principal" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" TextAlignment="Center"/>
            <Button Content="Abrir Chamado" Command="{Binding AbrirChamadoCommand}" Margin="0,10"/>
            <Button Content="Consultar Chamados" Command="{Binding ConsultarChamadoCommand}" Margin="0,10"/>
            <Button Content="Relatórios" Command="{Binding RelatoriosCommand}" Margin="0,10"/>
        </StackPanel>
    </Grid>


</Window>

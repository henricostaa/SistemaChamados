<Window x:Class="SistemaChamados.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaChamados.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    
    
    <Grid Margin="20">
        <StackPanel>
            <TextBox Text="{Binding Usuario}" Margin="0,0,0,10" />
            <PasswordBox x:Name="SenhaBox" PasswordChanged="SenhaBox_PasswordChanged" Margin="0,0,0,10" />
            <Button Content="Entrar" Command="{Binding EntrarCommand}" />
        </StackPanel>
    </Grid>

</Window>

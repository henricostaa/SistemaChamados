<UserControl x:Class="SistemaChamados.Views.TicketsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SistemaChamados.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="772"
             Style="{StaticResource Page_Style}">

            <UserControl.DataContext>
                <vm:TicketsVM />
            </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Lista de Tickets -->
        <Border Grid.Column="0" Margin="10" Background="White" CornerRadius="10" Padding="10" 
            Effect="{StaticResource CardShadow}">
            <StackPanel>
                <TextBlock Text="Tickets Criados" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10" />
                <ListBox ItemsSource="{Binding Chamados}" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F9F9F9" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Titulo}" FontWeight="Bold" FontSize="14" />
                                    <TextBlock Text="{Binding Descricao}" FontStyle="Italic" FontSize="12" Foreground="#666" />
                                    <TextBlock Text="{Binding Status}" Foreground="#888" FontSize="12" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Formulário de Criação -->
        <Border Grid.Column="1" Margin="10" Background="White" CornerRadius="10" Padding="10"
            Effect="{StaticResource CardShadow}">
            <StackPanel>
                <TextBlock Text="Novo Ticket" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10" />

                <StackPanel>
                    <TextBlock Text="Titulo" FontSize="14" Margin="0,0,0,5"/>
                    <Border CornerRadius="6" BorderBrush="#CE93D8" BorderThickness="1" Background="#F3E5F5">
                        <TextBox Text="{Binding Titulo}"                            
                                 Height="35"
                                 FontSize="14" 
                                 Padding="8"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="#4A148C"                
                                 CaretBrush="#4A148C"/>
                    </Border>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Descrição"  FontSize="14" Margin="0,0,0,5"/>
                    <Border CornerRadius="6" BorderBrush="#CE93D8" BorderThickness="1" Background="#F3E5F5">
                        <TextBox Text="{Binding Descricao}"
                                 Height="35"
                                 FontSize="14" 
                                 Padding="8"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="#4A148C"                
                                 CaretBrush="#4A148C"/>
                    </Border>
                </StackPanel>

                <StackPanel>
                    <ComboBox SelectedItem="{Binding Status}" Margin="0,10,0,10" Height="30">
                        <ComboBoxItem Content="Aberto" />
                        <ComboBoxItem Content="Fechado" />
                        <ComboBoxItem Content="Pendente" />
                    </ComboBox>
                </StackPanel>
                
                <Button Content="Criar Ticket" Command="{Binding CriarChamadoCommand}" 
                        Width="160" Height="35"
                        Style="{StaticResource ModernLoginButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
